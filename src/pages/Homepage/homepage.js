//generate QR code is only temporary here so I can view the design of this
import GenerateQR from "../../components/Generate QR/generateQR";

const Homepage = (props) => {
  return (
    <section>
      <br />
      <h1>Homepage</h1>
      {/* <GenerateQR /> */}
    </section>
  );
};

export default Homepage;
